import { h } from '../airship.core.js';

function borderStyleCounts(borderStyle) {
    return ['none', 'initial', 'inherit', 'hidden', 'unset'].indexOf(borderStyle) === -1;
}

export { borderStyleCounts as a };
