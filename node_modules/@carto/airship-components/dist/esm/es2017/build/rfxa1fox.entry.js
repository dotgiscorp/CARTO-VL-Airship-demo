import { h } from '../airship.core.js';

class LegendSizeCategory {
    constructor() {
        this.orientation = 'vertical';
        this.width = null;
    }
    render() {
        if (!this.data || this.data.length === 0) {
            return null;
        }
        return h("as-legend-size-bins-point", { data: this.data, orientation: this.orientation, width: this.width });
    }
    static get is() { return "as-legend-size-category-point"; }
    static get properties() { return {
        "data": {
            "type": "Any",
            "attr": "data"
        },
        "orientation": {
            "type": String,
            "attr": "orientation"
        },
        "width": {
            "type": Number,
            "attr": "width"
        }
    }; }
    static get style() { return "as-legend-size-category-point{display:block}"; }
}

export { LegendSizeCategory as AsLegendSizeCategoryPoint };
